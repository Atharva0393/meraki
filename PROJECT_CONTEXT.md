# Meraki Designs — Project Context

**Read this file before making any changes.** It exists to let a fresh Claude
Code session continue this project without losing established decisions,
design direction, or implementation state. This file was generated near a
context-window limit to hand off cleanly.

---

## 1. Project

- **Client:** Meraki Designs — architectural design + property development
  studio based in Derby, United Kingdom.
- **Deliverable:** Premium architectural studio portfolio / lead-generation
  website.
- **Directory:** `/Users/atharva/meraki`
- **Stack:** Next.js 16.3 (App Router), React 19, TypeScript, Tailwind CSS 4,
  Node 24 LTS (via nvm), npm, git (no remote). No CMS, no database, no auth,
  no animation library — all motion is hand-rolled CSS + a small
  IntersectionObserver component (see §7).
- **Dependencies are intentionally minimal:** `next`, `react`, `react-dom`
  only at runtime; `tailwindcss`, `typescript`, `eslint` + Next's ESLint
  config as dev deps. Nothing else has been added. Keep it that way unless
  there's a genuine need.
- **`ui-ux-pro-max` skill** is installed at `.claude/skills/ui-ux-pro-max`
  and was used once, early on, to seed `design-system/meraki-designs/MASTER.md`
  with a starting palette/type system (see §3 — the palette was manually
  corrected afterward; the skill's raw output was wrong for this brand).

---

## 2. Design direction (the brief, condensed)

The site must read as: **premium, architectural, editorial, calm,
sophisticated, minimal, intentional, cinematic, expensive without being
flashy.** Reference point: *Apple product-page restraint × high-end UK
architecture studio × editorial print design.*

Explicitly **not**: SaaS, generic agency template, AI-generated-looking,
overly animated portfolio, startup landing page, Webflow template,
glassmorphism, neon, futuristic/AI aesthetics.

Governing principle: **less, but better.**

**Visual system:**
- Background: warm ivory (`--color-ivory: #f6f1ea`)
- Secondary surface: stone (`--color-stone: #ede6da`) — used for the hero's
  outer backdrop band and image placeholders
- Primary text: near-black charcoal (`--color-charcoal: #211f1c`,
  `--color-ink: #1c1a17`)
- Accent: muted terracotta/copper clay (`--color-clay: #a9673f`) — used
  sparingly (CTA fills, active nav underline, small accent rules), never as
  a dominant fill
- Secondary text: warm grey (`--color-warm-grey: #6b655c`)
- Dividers: hairline (`--color-hairline: #ded5c4`)
- Type: **Fraunces** (editorial serif, headlines) + **Inter** (sans, body/UI),
  loaded via `next/font/google` in `src/app/layout.tsx`
- Generous whitespace, thin borders/dividers, pill-shaped buttons and tags.
  Rounded corners used selectively (buttons, tags, the hero's own card
  frame) — the site should still read as editorial, not card-based.
- Explicitly avoid: heavy shadows, SaaS card grids, gradients, glassmorphism,
  decorative/unnecessary icons, fake stats/testimonials/client logos.

All tokens live in `src/app/globals.css` (`:root` + Tailwind v4 `@theme
inline` block) and are documented in
`design-system/meraki-designs/MASTER.md`.

---

## 3. Design system doc

`design-system/meraki-designs/MASTER.md` is the source of truth for colors,
type, spacing, button/tag component specs, motion notes, and anti-patterns.
It was originally generated by the `ui-ux-pro-max` skill but the skill
mis-categorized the project ("Photography Studio") and proposed a pink
accent + wrong fonts — that was manually overridden early in the project
with the palette/type described in §2. If you regenerate anything from the
skill again, **do not trust its color/font output blindly** — sanity-check
against this file first.

Button convention (already implemented, see `pill-button.tsx`,
`navbar.tsx`): pill-shaped (`rounded-full`). `PillButton` takes a
`variant?: "clay" | "black"` prop (default `"clay"`):
- **"Start a Project" specifically** = `variant="black"` — solid charcoal
  fill, hover → clay. Explicit user instruction, deliberately reversed from
  the original clay-primary convention. Every real "Start a Project"
  instance (navbar ×2, hero ×2, footer) uses this.
- **Everything else** (e.g. the contact form's "Send Enquiry") = default
  clay fill, hover → charcoal — the original convention, unchanged.
- Plain nav/secondary actions elsewhere = solid charcoal fill, hover → clay
  (unchanged).

---

## 4. Site structure (all routes exist and are built)

| Route | File | Status |
|---|---|---|
| `/` | `src/app/page.tsx` | Hero + Selected Projects teaser |
| `/work` | `src/app/work/page.tsx` | Editorial hero + coverflow project carousel (`components/ui/coverflow-carousel.tsx`) + project index |
| `/projects` | `src/app/projects/page.tsx` | PageHero + ProjectGallery (all 3 projects, asymmetric editorial layout) — **orphaned**: no longer linked from nav or footer (superseded by `/work`), but still built and reachable directly |
| `/projects/[slug]` | `src/app/projects/[slug]/page.tsx` | Placeholder detail page per project (statically generated via `generateStaticParams`) |
| `/services` | `src/app/services/page.tsx` | PageHero + 4-service NumberedList |
| `/contact` | `src/app/contact/page.tsx` | PageHero + ContactForm + contact-info block |

**`/studio` was deleted** (explicit user instruction) — the route, its page
file, and `studio-intro.tsx` (the homepage's "The Studio" section) are gone.
If you see a reference to either in an older note, it's stale.

Navbar (`src/components/navbar.tsx`): logo → `/`, links → Work/Services/
Contact, primary CTA "Start a Project" → `/contact` (black, see §3).
Active-page underline indicator. Full-screen mobile menu (see §7).

**Footer** (`src/components/footer.tsx`) — built, mounted once in
`src/app/layout.tsx` as a sibling after `PageTransition` (so it persists
across route changes without re-triggering the page-fade). Dark charcoal
upper field with an asymmetric roofline `clip-path`, an oversized ivory CTA
panel overlapping it, contact placeholders, a real Instagram link, and a
minimal bottom bar. `ClosingCta` (`closing-cta.tsx`) was retired in favour
of this — it's still in the tree but unused everywhere; treat it as dead
code, not a live pattern.

---

## 5. Homepage — approved, do not redesign without explicit instruction

### Hero (`src/components/hero.tsx`)
Composition has been through many iterations and is **approved**. Content:
- Eyebrow: "MERAKI DESIGNS — DERBY, UK"
- Headline: "Spaces designed / *with intention.*" (second line italic)
- Supporting copy: "Architectural design, property development and
  thoughtful spaces created around the way you live."
- Primary CTA: "Start a Project" (pill, clay) → `/contact`
- Secondary: "Explore Our Work →" → `#work` (anchor doesn't resolve to
  anything yet — harmless, low priority to fix)
- Right-side pill tags: "Architectural" (with small house-icon badge),
  "Bespoke", "Sustainable" — decorative service pillars, not real stats
- Desktop: whole hero sits in a rounded card (`rounded-[2rem]`) on a stone
  backdrop band; nav integrated at the top of the card; headline overlaps
  the top of the architectural image (roofline grazes the italic line —
  intentional, tuned to stay legible); image is scaled up via CSS
  `scale-150` transform (not container resize) to read as dominant/large
  while keeping the box's layout size unchanged
  (see §6 for why that specific mechanism was used)
- Mobile: fully restacked (not just shrunk) — text block first, image
  below, no overlap, own set of tags

**Do not** re-crop the image, change the overlap amount, resize the image
container, or alter this composition without the user explicitly asking.
This took several rounds of back-and-forth to get right.

### Selected Projects (`src/components/selected-projects.tsx`)
Teaser for the homepage: eyebrow "SELECTED WORK" + headline "Spaces with
purpose." + asymmetric 3-card layout (project 1 full-width dominant,
projects 2–3 staggered pair below) + "View all projects →" → `/projects`.
Has its own local `ProjectVisual`/`ProjectCard` (intentionally **not**
shared with `project-visual.tsx`/`project-gallery.tsx` — kept separate so
edits to the full `/projects` page can't accidentally break this approved
homepage section, and vice versa). Each card is capped at `max-w-[90%]`
(image + text together, not just the image) and the image container has
`rounded-2xl` — explicit user instruction ("images smaller, rounded
edges"). Approved, motion-polished (§7).

---

## 6. The hero image — history, don't regress

File: `public/images/hero/hero-home.webp` (referenced via
`src/lib/media.ts`). Original source: `bgimage.png` in the project root
(6144×4096, untouched, kept as raw source).

What happened, in order — useful if the image ever needs reprocessing:
1. Original PNG had ~25% of empty sky above the roofline and generous
   side margins.
2. Cropped tightly to the actual visible building bounds (roughly
   `left,top,right,bottom = 1067,1251,5169,4096` in the original's pixel
   space) so the building fills nearly its whole frame.
3. Background-removed via a color-distance alpha mask (Python/Pillow,
   sampling the corner background color, thresholding by distance) to get
   a genuine transparent cutout — not just a matching flat color — so the
   asset can sit directly on the ivory page/card background with no visible
   "card" seam, and so headline text can overlap the roofline without a
   rectangular box being visible.
4. Background tone color-corrected to match the `--color-ivory` token
   exactly (small RGB shift).
5. Final asset: 2800×1942 WebP with alpha, ~1MB.

**Why the hero image uses a CSS `scale-150` transform instead of a bigger
container:** the user explicitly required the *visible size* of the
building to increase without changing the container's layout box (which
would shift the CTA row position / hero height). `transform: scale()` (in
Tailwind v4, compiled to the standalone CSS `scale` property, not
`transform`) changes only painted pixels, not layout — exactly what was
needed. **Do not "fix" this by just enlarging the image div** — that was
tried and explicitly rejected by the user for changing spacing elsewhere.

---

## 7. Motion system (built and verified this session — not upcoming work)

**Important correction vs. any older notes:** the "visual polish + motion"
phase described below is **already implemented, tested, and passing
lint/typecheck/build** — it is not a future task. If resuming from an older
summary that says "Motion: NEXT PHASE," that summary is stale.

Core primitive: `src/components/reveal.tsx` — `"use client"` component,
IntersectionObserver-based (fires once, `threshold: 0.15`), toggles an
`is-visible` class that a CSS transition picks up. Two variants:
`variant="up"` (fade + translateY, default) and `variant="scale"`
(fade + scale, used for images). Delays via `delayMs` prop
(`style={{transitionDelay}}`) for staggering.

CSS in `src/app/globals.css`:
- `.animate-fade-up`, `.animate-reveal-image` — original **load-triggered**
  keyframe animations, still used by the hero only (already had motion
  before this phase; left alone).
- `.animate-page-fade` — **opacity-only** load animation used by
  `PageTransition` for route changes. **Important:** this used to include a
  `transform: translateY()` — that was a real bug (see below) and was
  removed. Do not re-add a transform here.
- `.reveal` / `.reveal-scale` (+ `.is-visible`) — the scroll-triggered
  transition-based classes `Reveal` toggles.
- Everything respects `prefers-reduced-motion: reduce` (one consolidated
  media query at the bottom of the file disables all animation/transition
  classes and snaps to end-state).

Where motion was added:
- `src/components/page-transition.tsx` — wraps `{children}` in
  `src/app/layout.tsx`, keyed by `usePathname()`, so every route change
  re-mounts and re-fades content in (~400ms, opacity only).
- `src/components/site-header.tsx` — sticky (`sticky top-0`) on the four
  interior pages, with a scroll listener toggling a blurred/hairline-bordered
  background once `scrollY > 8`. **Homepage hero's own nav is intentionally
  NOT sticky** — making it sticky would break the approved rounded-card
  composition (nav would visually detach from the card once stuck), so this
  was a deliberate scope decision, not an oversight.
- `src/components/navbar.tsx` — real full-screen mobile menu (slide/fade
  overlay, header row incl. close button stays visible above it via
  `z-40`), animated underline on desktop nav links, active-page indicator.
- `src/components/pill-button.tsx`, `arrow-link.tsx` — hover/press
  micro-interactions (subtle lift + scale on press; grow-in underline on
  links).
- `src/components/numbered-list.tsx` (shared by Studio principles and
  Services list), `project-gallery.tsx`, `project-visual.tsx`,
  `selected-projects.tsx`, `studio-intro.tsx`, `page-hero.tsx`,
  `closing-cta.tsx`, `contact-form.tsx` — all wrapped with `Reveal` and/or
  given hover-state refinements (numeral color shift, title translate,
  link opacity).

### Two real bugs found and fixed this session (worth knowing about)
1. **Fixed-position elements broke sitewide.** `animate-page-fade`
   originally used `transform: translateY()`. Any element with a non-`none`
   transform becomes a CSS containing block for `position: fixed`
   descendants. Since `PageTransition` wraps the entire page, this silently
   broke the mobile nav overlay (and would break any other `fixed` element)
   everywhere. Fixed by making that animation opacity-only. **If you ever
   add a transform-based animation to a wrapper high in the tree, check
   this doesn't happen again.**
2. **Duplicate DOM IDs.** `Navbar` is rendered *twice* by `hero.tsx`
   (desktop variant + mobile variant, toggled via Tailwind breakpoints —
   this predates the motion work and wasn't changed). Once `Navbar` gained
   a mobile-menu overlay with a static `id="mobile-nav"`, having two
   instances meant duplicate IDs and `aria-controls` pointing at the wrong
   (hidden) instance. Fixed with `useId()` for a collision-free ID per
   instance. If you ever add more stateful IDs to `Navbar`, remember it can
   mount more than once per page.

### Known testing-environment limitation (not an app bug)
The `:focus` pseudo-class could not be reliably triggered via this
session's browser-automation tooling (`element.focus()` updates
`document.activeElement` but doesn't always trigger real CSS `:focus`
matching in that environment; real click-to-focus was also unreliable
there). The contact form's focus-underline CSS was verified correct by
reading the compiled Tailwind output directly (correct selector, correct
specificity) rather than by a live screenshot. If a fresh session has a
working browser tool, it's worth a quick real visual check, but there's no
reason to suspect the CSS itself is wrong.

---

## 8. Projects data — stock photography in use, not placeholder anymore

`src/lib/projects.ts` exports a typed `projects` array:

```ts
export type Project = {
  slug: string;
  title: string;
  location: string;
  category: string;
  image?: { src: string; alt: string }; // omitted = placeholder still in use
};
```

**Status change, explicit user instruction:** all 3 projects now have
`image` populated with verified real Unsplash stock architectural
photography (`images.unsplash.com`, allow-listed in `next.config.ts` via
`images.remotePatterns` — required for `next/image` to load them). This
directly reverses the original "never invent project photography" rule
below — flagging that plainly rather than burying it, because it's a real
tension worth knowing about:

**On a live site, this reads as a claim that these are real Meraki
projects, and they aren't.** The user was told this explicitly (once for
the `/work` page in an earlier session, once for this site-wide pass) and
chose to proceed both times. If a future session is asked to "add more
projects" or "add photography," the same stock-photo approach is the
established pattern now — but if the user ever asks *why* a project photo
looks generic/unfamiliar, this is why, and swapping in real photography
should be treated as a standing to-do, not a surprise.

The same 3 images are deliberately reused across `/`  (Selected Projects),
`/projects`, `/projects/[slug]`, **and** `/work` (which has its own
separate hardcoded slide list in `work/page.tsx`, not driven by this file)
— same building per project, not a different random photo per page.

| # | Title | Location | Category | Slug |
|---|---|---|---|---|
| 01 | Contemporary Residence | Derby, UK | Residential Architecture | `contemporary-residence` |
| 02 | Bespoke Living | Nottingham, UK | Residential Design | `bespoke-living` |
| 03 | The Modern Extension | Derbyshire, UK | Extension & Refurbishment | `the-modern-extension` |

If `image` is ever unset again (e.g. a newly added project before real
photography exists), `ProjectVisual` (`project-visual.tsx`, and the local
copy inside `selected-projects.tsx`) falls back to a stone-colored
placeholder with a large faint serif numeral + "Photography to follow" —
keep that fallback pattern, don't delete it.

Contact page (`src/app/contact/page.tsx`) placeholders, same rule —
**never invent real contact details**:
```
Email:    [Add email address]
Phone:    [Add phone number]
Location: Derby, United Kingdom   ← this one is real/given, not a placeholder
```

---

## 9. Project philosophy / working rules

The user has repeatedly emphasized: **don't overcomplicate this, fewer
prompts, clean and premium execution over technical completeness.**

1. Read this file before changing anything.
2. Inspect current implementation before modifying it (read the actual
   component, don't assume from memory).
3. Preserve approved decisions (hero composition, image treatment, palette)
   unless the user explicitly asks to revisit them.
4. Don't rebuild working pages without a clear reason.
5. Never invent business info or contact details — use the established
   placeholder patterns instead. Project *photography* is the one
   exception in practice now (§8) — the user explicitly instructed stock
   photos site-wide despite this rule; the rule still holds for everything
   else (fake stats, fake testimonials, fake client names, fake contact
   info).
6. Keep the visual system consistent site-wide (reuse `Eyebrow`,
   `ArrowLink`, `PillButton`, `PageHero`, `NumberedList`, `Reveal` rather
   than inventing new one-off patterns).
7. Motion stays subtle/premium: opacity + transform only, ~500–900ms major
   reveals, ~200–400ms micro-interactions, no bounce, no parallax, respect
   `prefers-reduced-motion`.
8. Don't add dependencies unless genuinely necessary — this project has
   deliberately stayed on zero UI/animation libraries.
9. Before calling UI work done: actually load it in the browser (desktop +
   mobile), not just code review. Run `npm run lint`, `npx tsc --noEmit`,
   `npm run build` and confirm all pass, and check for browser console
   errors.
10. Prefer fewer, larger, meaningful changes over many small ones.

---

## 10. Current status

| Area | Status |
|---|---|
| Foundation (Next.js/TS/Tailwind setup, fonts, tokens) | Complete |
| Homepage hero | **Approved** — do not redesign without explicit ask |
| Homepage Selected-projects teaser | Built, motion-polished, real (stock) imagery, `max-w-[90%]` + `rounded-2xl` |
| `/studio` | **Deleted** — route, page, and `studio-intro.tsx` all removed |
| `/work` | Built — coverflow carousel, real (stock) imagery |
| `/projects` + `/projects/[slug]` | Built, motion-polished, real (stock) imagery — `/projects` itself is orphaned from nav (see §4) |
| `/services` | Built, motion-polished |
| `/contact` | Built, motion-polished (form is client-side only — no backend/email wiring exists yet) |
| Navbar | Built — Work/Services/Contact, sticky (interior pages), mobile menu, active state, hover motion |
| Footer | Built — see §4 |
| Sitewide motion system | Built and verified (§7) — not a pending task |
| Contact form backend | **Not built** — submitting shows a client-side "Thank you" state only; nothing is actually sent anywhere |

### Natural next steps (not started, not requested yet)
- Real contact form submission (needs an API route or third-party form
  service — will require picking an approach with the user first)
- Real project photography to replace the stock images (§8)
- Fixing the `#work` dead anchor if it still exists in `hero.tsx` — worth a
  grep, this note has been stale before
- Deciding whether the orphaned `/projects` route should be removed,
  redirected to `/work`, or left as-is

---

## 11. Design reference skills (`.claude/skills/`)

Two reference skills are installed. They are **advisory references, not
stylesheets** — neither may override the Meraki brand identity in §2.

| Skill | Role |
|---|---|
| `ui-ux-pro-max` | General UI/UX reference — layout, product patterns, accessibility, component conventions, charts. Installed first; see §3 caveat about its unreliable color/font output for this brand. |
| `apple-design` | Apple-specific reference for interaction quality, motion physics, typography detail, materials/depth, and design foundations. Vendored unmodified from `emilkowalski/skills` (MIT) — see `.claude/skills/apple-design/SOURCE.md`. |

### The hard rule: brand identity wins

**The Meraki site must not become a copy of Apple's website.** Apple Design is
a source of *craft*, never of *appearance*.

Take from `apple-design`: restraint, hierarchy, whitespace discipline,
typography scale (size-specific tracking/leading), interaction quality, subtle
and interruptible motion, responsive behaviour, transition symmetry,
micro-interactions, clarity, and the reduced-motion/accessibility rules.

Never take from it anything that displaces §2: Meraki keeps its **ivory/stone
palette**, **charcoal typography**, **restrained clay/terracotta accent**,
**Fraunces serif + Inter sans** pairing, and its **editorial architecture-studio**
character. If a rule in either skill conflicts with §2 or §9, §2 and §9 win.

### Specific carve-outs where the skills conflict with this project

- **Do not add an animation library.** `apple-design` recommends
  Motion/Framer Motion for springs. §1 and §9.8 keep this project at zero
  UI/animation dependencies. Use it as *theory* for the hand-rolled CSS +
  `Reveal` system (§7); adding the dependency needs an explicit decision.
- **Do not adopt `system-ui` as the type face.** `apple-design` §15 says
  default to the system font. Meraki has a deliberate Fraunces/Inter identity
  (§2). Take the *tracking/leading discipline* from that section, not the
  font recommendation.
- **Do not introduce glassmorphism.** `apple-design` §12 promotes translucent
  `backdrop-filter` materials. §2 explicitly lists glassmorphism as an
  anti-pattern. The one sanctioned use is the existing subtle sticky-header
  blur in `site-header.tsx`; do not extend it into a system.
- **Do not add heavy shadows, dimming scrims, or stacked-sheet depth** as a
  general pattern — §2 rules out heavy shadows and card-based UI.
- **Rounded corners stay selective** (buttons, tags, hero card). Apple's
  continuous-corner language does not license rounding everything.

### Where `apple-design` genuinely applies here

Motion timing and easing, gesture/drag feel if any is added, hover and press
micro-interactions, transition symmetry (enter/exit along the same path),
`transform-origin` anchoring, respecting `prefers-reduced-motion`, type
tracking that tightens as headings grow, and the "every value is a deliberate
choice you can defend" standard of craft.
